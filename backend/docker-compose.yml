version: '3.8'

services:
  concierge-agent:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ~/.config/gcloud:/root/.config/gcloud:ro
    restart: unless-stopped

